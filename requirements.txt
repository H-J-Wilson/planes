# Use PyPI directly.
# Some Raspberry Pi OS installations configure piwheels as the package index.
# The piwheels Jinja2 3.1.6 and typing-extensions 4.16.0 wheels currently
# have package-name metadata that pip can reject as inconsistent.
--index-url https://pypi.org/simple

fastapi>=0.110.0
uvicorn[standard]>=0.28.0
requests>=2.31.0
jinja2>=3.1.0
pytest>=8.0
